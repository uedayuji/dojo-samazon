<h1>Products#new</h1>

<% # Rails 5.2?からform_withに統合 %>
<%= form_with model: @product do |f| %>
  <%= f.text_field :name %>
  <%= f.text_area :description %>
  <%= f.number_field :price %>
  <%= f.collection_select(:category_id, @categories, :id, :name) %>
  <%= f.submit "Create" %>
<% end %>

<%= link_to "Back", products_path %>